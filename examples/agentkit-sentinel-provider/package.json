{
  "name": "agentkit-sentinel-provider-prototype",
  "private": true,
  "version": "0.1.0",
  "description": "Local Action Provider-style prototype for Sentinel risk checks (not official, not submitted)",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@beezshield/sentinel": "^0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
